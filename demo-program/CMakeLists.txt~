file(GLOB myprog_source "*.c" "*.cpp" "*.h")

#add_library(myprog ${myprog_source} )


if (CMAKE_SYSTEM_NAME STREQUAL Windows)
  target_link_libraries(base udebug iphlpapi psapi)
else()
  target_link_libraries(base udebug)
endif()

add_executable(ourprog myprog.cpp)
target_link_libraries(ourprog cdd dbm hash)


